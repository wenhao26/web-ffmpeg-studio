# Storage Policy & Lifecycle Management: Web FFmpeg Studio

## 1. 概述与设计目标
音视频处理与转码属于典型的**高磁盘吞吐与占用型业务**。为了防止临时文件堆积、大体积转码产物撑满服务器磁盘，以及保障数据安全性与隐私合规，系统必须制定严格的**存储分区、生命周期管理与自动化清理策略**。

---

## 2. 存储目录层级与隔离规范

服务器后端的 `storage/` 目录必须严格划分为以下三个独立子域：

```text
storage/
├── inputs/         # 用户上传的原始媒体暂存区
├── outputs/        # 处理完成的最终成品产物区（如转码后视频、HLS切片包）
└── temp/           # 运行时临时工作区（如分片中间文件、提取的帧图片）
