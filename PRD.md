# Product Requirements Document (PRD): Web FFmpeg Studio (Enterprise Edition)

## 1. 项目概述与定位
本项目是一个面向开发者、内容创作团队及企业的**企业级 FFmpeg 在线可视化控制台与指令编排平台**。旨在将复杂的 FFmpeg 命令行参数通过严谨、模块化的可视化界面进行封装，支持从基础媒体探测、常规转码到高阶复杂指令组合（如多轨滤镜、复杂剪辑、流媒体切片、画中画等）的全方位企业级生产操作，同时兼顾系统安全性、高并发健壮性与极高的代码可维护性。

---

## 2. 核心功能模块与复杂度设计

### 2.1 媒体资产探测与元数据深度分析（FFprobe Engine）
- **全面元数据深度解析**：对上传的音视频文件进行深度探测，输出结构化数据（封装格式、总时长、精确码率、分辨率、色彩空间、帧率 FPS、各音视频轨道的编码格式、声道与采样率）。
- **流状态安全校验**：自动检测媒体流完整性，对文件损坏、编码不兼容或含有恶意畸形数据的视频实施前置拦截与错误抛出。

### 2.2 全面且可扩展的 FFmpeg 指令操作矩阵（从基础到专家）
系统架构必须具备高度的可扩展性，支持以下梯度的企业级指令编排：
1. **基础转码与流控制层**：
    - 多格式互转（MP4, WebM, MKV, MOV 等）。
    - 编码器自主选择（H.264, H.265/HEVC, VP9, AV1，音频 AAC, MP3, Opus）。
    - 质量与码率精细化控制（CRF 模式、平均码率、最大码率及缓冲区大小设定）。
2. **常规编辑与几何调整层**：
    - 空间裁剪（Crop）与等比缩放、黑边填充（Pad）。
    - 高精度时间轴剪辑（支持指定 `-ss` 与 `-to`/`-t`，支持关键帧对齐）。
3. **高级视音频特效与复合滤镜层**：
    - 多媒体混流与轨道替换（视频去音频、多音轨合并）。
    - 水印合成：支持动态图片水印、自定义文字水印（支持指定绝对坐标、透明度、字体、渐变或淡入淡出特效）。
    - 复合滤镜图（Filtergraphs）：支持复杂的 `-filter_complex` 语法（如画中画 PiP、多画面宫格拼接）。
4. **流媒体与专家高级模式**：
    - 一键生成 HLS（`.m3u8` + `.ts` 分片）或 DASH 生产级切片，满足网页点播与移动端自适应码流需求。
    - 专家自由指令模式：允许高级开发人员输入自定义扩展参数，并在后端安全白名单校验下直接运行。

### 2.3 可视化界面与交互设计（Frontend UI/UX）
- **双向联动指令实时构建器**：用户在前端通过表单、滑块、坐标选择器调整参数时，下方的“原始 FFmpeg 命令行”区域必须实时高亮、动态拼接生成对应指令，所见即所得。
- **现代化生产级控制台**：采用 Vue 3 + 企业级 UI 组件库，支持亮/暗主题、参数分组折叠、多步骤引导。
- **实时执行状态与日志终端**：异步或同步执行过程中，前端通过日志终端实时展示 FFmpeg 的标准错误输出（Stderr），解析并可视化渲染当前转码进度百分比、处理帧率（fps）及剩余时间估算。
- **资产预览与生命周期追溯**：处理成功后支持在线流畅预览（Video Player）、一键安全下载、文件体积压减前后对比以及执行历史追溯。

---

## 3. 企业级非功能性需求（NFR）

### 3.1 安全防护与资源沙箱（Security & Guardrails）
- **严格防命令注入**：所有参与拼装的动态参数必须通过后端强类型校验，并强制调用系统级转义函数，严禁任何形式的 shell 注入漏洞。
- **安全白名单与格式校验**：禁止依赖后缀名判断文件类型，必须通过二进制文件头（Magic Number）及 MIME-Type 双重白名单校验。
- **资源护栏与软隔离**：单次处理文件设置严格的体积上限、执行超时控制（Timeout），防止死循环或超大文件拖垮服务器 CPU 与磁盘。

### 3.2 存储与文件生命周期管理（Storage Lifecycle）
- 规范化划分服务器存储目录（独立隔离输入目录、输出目录及临时工作目录）。
- 建立自动化清理机制：对过期或历史生成的音视频产物设定定时清理策略（如超过设定小时数自动删除），防止磁盘空间溢出。

### 3.3 企业级编码规范与架构设计
- **后端（PHP 8.x）**：采用严格类型声明（`declare(strict_types=1);`），基于面向对象设计原则（SOLID）、依赖注入及解耦的 Service-Builder 模式，确保指令生成逻辑高度单元测试友好。
- **前端（Vue 3）**：采用 Composition API 与 Pinia 状态管理，组件职责单一，表单校验严密，具备完善的异常捕获与友好提示机制。
